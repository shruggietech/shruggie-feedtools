# shruggie-feedtools
Python module that normalizes web feeds (RSS, Atom, JSON Feed, WordPress REST) into a single predictable JSON schema, and constructs schema-compliant feeds from raw text via templates. Ships as a Python package, CLI tool, and Windows GUI app.
